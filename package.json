{
  "name": "screenblocker",
  "version": "0.1.0",
  "description": "Stand-up & stretch break enforcer for KDE Plasma / Wayland",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "APPIMAGE_EXTRACT_AND_RUN=1 NO_STRIP=true tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
